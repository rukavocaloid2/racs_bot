Flask>=2.0
google-cloud-aiplatform>=1.40.0  # Or a more recent version
protobuf                   # Often needed with Google Cloud libraries
gunicorn                   # WSGI server for production on Heroku
python-dotenv              # Useful for local development with .env file
vertexai                   # New preferred library for Gemini models on Vertex